# Claude Code

Follow `AGENTS.md`; it is authoritative. This project is research-only, secret-free in logs/persistence, TDD-first, and immutable at runtime. Use `python3.12 scripts/bootstrap.py --dev`, then `.venv/bin/trade-research doctor`. The shared plugin is `plugins/trade-research`; its MCP launcher exposes bounded research tools only.
