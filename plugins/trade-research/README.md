# Trade Research plugin

Codex and Claude discover the same five research workflows and `.mcp.json`. The launcher resolves the checkout without fixed user paths and starts only the bounded stdio MCP server. Install the repository first; the plugin never provisions secrets.
